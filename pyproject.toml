[project]
name = "pupu-cafe-set"
version = "0.1.0"
description = "Survey site for reviewing cutlery counts on high-diff cafe orders"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []
